<h1> Hip Hop Savant </h1>

<a href="<%= new_artist_url %>"> Add an Artist </a>
<br>
<a href="<%= new_song_url %>"> Add a Song </a>
<br>

<h2> Some Songs</h2>
<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="<%= songs_url(song) %>">"<%= song.title %>" by <%= song.artist.name %></a>
    </li>
  <% end %>
</ul>

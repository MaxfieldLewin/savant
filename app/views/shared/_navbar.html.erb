<% if logged_in? %>
Welcome, <%= current_user.username %>
<br>
<form action="<%= session_url %>" method="post">
  <%= form_auth_token %>
  <%= form_http_method("delete") %>
  <button>Log Out</button>
</form>
<br>
<% else %>
<a href="<%= new_session_url %>"> Log in </a>
<br>
<% end %>
